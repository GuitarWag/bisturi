#!/usr/bin/env python3
"""Standalone launcher so `./bisturi` works without installing the package."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from ctxbisturi.cli import main  # noqa: E402

if __name__ == "__main__":
    raise SystemExit(main())
